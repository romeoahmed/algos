# 多数元素

## 问题

输入为非空整数序列，保证某个值出现次数严格超过 n/2。返回该多数元素。

## 思路

Boyer–Moore 投票法维护候选值 c 和余额 b。每读入一个值 x，若 b = 0，先令 c = x；随后 x = c 时余额加 1，否则减 1。

余额是抵消不同元素后剩余的候选数量。

## 伪代码

[记号约定](../notation.md)。

```text
MAJORITY(A)
    c ← 0; b ← 0
    for x in A
        if b = 0
            c ← x
        if x = c
            b ← b + 1
        else
            b ← b - 1
    return c
```

初始 c = 0 仅为占位，第一个元素会覆盖它。

## 示例

处理 `[2, 2, 1, 1, 1, 2, 2]` 时，状态 `(c, b)` 依次为：

```text
(2, 1) → (2, 2) → (2, 1) → (2, 0)
       → (1, 1) → (1, 0) → (2, 1)
```

最终候选为 2。

## 正确性

已扫描前缀始终可分成若干对不同元素和 b 个剩余的 c。每次投票都保持此性质。

抵消一对不同元素时，多数元素最多减少一个，其余元素至少减少一个，故多数优势仍在。扫描结束后，剩余候选必为多数元素。

## 复杂度与实现

单次遍历，时间 Θ(n)，额外空间 O(1)。

投票函数将旧状态和当前值映射为新状态。Python 用 `functools.reduce`，C++ 用 `std::ranges::fold_left`，均按输入顺序折叠。Python 接受 `Iterable[int]`，会完整消耗迭代器输入。

参考：[Boyer–Moore 多数投票算法](https://www.cs.utexas.edu/~moore/best-ideas/mjrty/)。
