[project]
name = "algos"
version = "0.1.0"
requires-python = ">=3.14"

[dependency-groups]
dev = ["hypothesis>=6.168", "pytest>=9", "ruff>=0.16.6", "ty>=0.0.1"]

[tool.uv]
package = false

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4", "PIE", "RET", "RUF", "ANN", "PT", "PTH"]

[tool.ty.rules]
all = "error"
